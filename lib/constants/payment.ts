// Razorpay's checkout SDK requires a literal hex string — it cannot parse oklch().
// Keep in sync with --primary in app/globals.css (Rose Gold Boutique palette).
export const RAZORPAY_THEME_COLOR = "#9f422b";
